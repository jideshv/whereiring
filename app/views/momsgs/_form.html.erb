<%= form_for(@momsg) do |f| %>
  <% if @momsg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@momsg.errors.count, "error") %> prohibited this momsg from being saved:</h2>

      <ul>
      <% @momsg.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :from %><br />
    <%= f.number_field :from %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
